#ifndef HARDWARE
#define HARDWARE
#include "registers.h"

/*
 *  This file is for emulating hardware specific ideals.
 *  
 *  Such as: Sending/recieving data to/from ports,
 *           the GDT/IDT, and applying followng functionality,
 *           converting current emulator state from 16bit(BIOS) to 32bit,
 *           memory map, registers, memory addressing,
 *           memory segmentation etc etc
 * */

#endif